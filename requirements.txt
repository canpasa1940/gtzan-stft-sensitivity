# Analysis and verification only — TensorFlow is not needed to reproduce
# any table in the paper, because the model outputs are stored under data/.
numpy>=1.24
pandas>=2.0
scipy>=1.10
scikit-learn>=1.3
scikit-posthocs>=0.7
statsmodels>=0.14
matplotlib>=3.7

# Needed only to re-extract features or re-run inference from the saved models:
# librosa>=0.10
# onnxruntime>=1.16
# tensorflow==2.20.0
